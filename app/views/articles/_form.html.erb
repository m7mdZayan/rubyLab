
<%= form_with model: @article do |form| %>
    <div>
      <h3 class="alert alert-secondary mt-5"><%= form.label :title %></h3>
      
      <br>
      
      <%= form.text_field :title , :class => "form-control w-75 m-auto"%>
      <% @article.errors.full_messages_for(:title).each do |message| %>
        <div><%= message %></div>
      <% end %>
    </div>
  
    <div>
    <h3 class="alert alert-secondary mt-5"> <%= form.label :body%><br></h3>
      <%= form.text_area :body , :class => "form-control w-75 m-auto" %>
      <% @article.errors.full_messages_for(:body).each do |message| %>
        <div><%= message %></div>
      <% end %>
      </div>
        
    <div>
      <%= form.label :status %><br>
      <%= form.select :status, ['public', 'private', 'archived'], selected: 'public' %>
    </div>

    <div class="mt-4 mb-5">
      <%= form.submit :class => "btn btn-success" %>
    </div>


  <% end %>